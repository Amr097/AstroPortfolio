---
interface Props {
  text: string;
  icon?: any;
  [key: string]: any;
}

const { text, icon: Icon, ...attributes } = Astro.props;
---

<button
  class="group bg-surface-900 border-border-900 hover:border-border-300 hover:bg-surface-700 grid cursor-pointer grid-cols-[1fr_max-content] items-center justify-center gap-2 rounded-full border px-6 py-3 text-lg transition-all duration-200 ease-in-out hover:shadow-xs"
  {...attributes}
>
  <span class="text-inverse text-lg">{text}</span>
  {
    Icon && (
      <Icon
        size={24}
        class="text-inverse opacity-0 transition-opacity duration-200 group-hover:opacity-100"
      />
    )
  }
</button>
