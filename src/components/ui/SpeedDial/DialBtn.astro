---
const { icon, label } = Astro.props;
---

<button
  class="relative flex h-12 w-12 items-center justify-center rounded-full bg-white text-gray-700 shadow-lg"
  aria-label={label}
  set:html={icon}
/>
